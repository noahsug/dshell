;; (defun on-cc-loaded ()
;;   (subword-mode)
;;   (setq c-basic-offset 2)
;;   (require 'google-c-style)
;; )
;; 
;; (require 'auto-complete-c-headers)
;; (add-to-list 'ac-sources 'ac-source-c-headers)
;; 
;; (add-hook 'cc-mode-hook 'on-cc-loaded)
;; (add-hook 'c++-mode-hook 'on-cc-loaded)
